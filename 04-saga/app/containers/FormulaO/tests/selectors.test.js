// import { fromJS } from 'immutable';
// import { makeSelectFormulaODomain } from '../selectors';

// const selector = makeSelectFormulaODomain();

describe('makeSelectFormulaODomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
